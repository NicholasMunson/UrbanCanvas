import React from 'react'

const Directions = (props) => {
    return (
        <p>
				When you select an art piece that you would like to visit the map will load
        on the top and show the location
        </p>
    )

}

export default Directions